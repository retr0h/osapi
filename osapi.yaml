---
debug: false

client:
  url: http://0.0.0.0:8080

server:
  port: 8080
  security:
    cors:
      allow_origins:
        - http://localhost:3001
        - https://retr0h.github.io

queue:
  database:
    driver_name: sqlite3
    data_source_name: file:database.db?_journal=WAL&_timeout=5000&_fk=true
    database_name: osapi_jobs
